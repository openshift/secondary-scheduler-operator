apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-operand
  annotations:
    include.release.openshift.io/self-managed-high-availability: "true"
    include.release.openshift.io/single-node-developer: "true"
spec:
  podSelector:
    matchLabels:
      app: secondary-scheduler
  policyTypes:
  - Ingress
  - Egress
